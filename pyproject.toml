[project]
name = "cloudflare-best-ip"
version = "0.0.1"
description = "Cloudflare IP 优选工具"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
    "pyyaml>=6.0.3",
    "rich>=15.0.0",
]

[project.scripts]
best = "main:main"

[tool.uv]
package = false
