[project]
name = "cloudflare-best-ip"
version = "0.0.15"
description = "Cloudflare IP 优选工具"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "loguru>=0.7.3",
    "pydantic>=2.13.4",
]

[project.scripts]
best = "main:main"

[tool.uv]
package = false
